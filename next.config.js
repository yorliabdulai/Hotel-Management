/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com', 'avatars.githubusercontent.com','plus.unsplash.com', 'media.istockphoto.com', 'images.unsplash.com']
    }
}

module.exports = nextConfig
